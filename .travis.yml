sudo: required
services:
  - docker

before_install:
  - docker build =t shaggydog3/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run shaggydog3/react-test npm test -- --coverage

after_success:
  - docker build -t shaggydog3/multi-client ./client
  - docker build -t shaggydog3/multi-nginx ./nginx
  - docker build -t shaggydog3/multi-server ./server
  - docker build -t shaggydog3/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push shaggydog3/multi-client
  - docker push shaggydog3/multi-nginx
  - docker push shaggydog3/multi-server
  - docker push shaggydog3/multi-worker